package mmx;

class Block extends BlockHeader {
	
	vector<Transaction*> tx_list;
	
	
	void finalize();
	
	bool is_valid() const;
	
	// TOOD: bool full_hash
	hash_t calc_tx_hash() const;
	
	ulong calc_cost(ChainParams* params) const;
	
	BlockHeader* get_header() const;
	
	vector<Transaction*> get_all_transactions() const;
	
	void validate() const;
	
}
