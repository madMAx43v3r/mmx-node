package mmx.contract;

import mmx.hash_t;
import mmx.addr_t;
import mmx.ulong_fraction_t;

class Token extends mmx.Contract {
	
	string name;
	
	string symbol;
	
	string website;
	
	string icon_url;
	
	int decimals;
	
	ulong_fraction_t time_factor;							// for inflation / deflation (factor applied per block)
	
	Condition* stake_condition;
	
	map<addr_t, ulong_fraction_t> stake_minting;			// [contract => mint factor per block]
	
	
//	bool is_valid() const;
	
//	hash_t calc_hash() const;
	
}
