package mmx.contract;

import mmx.hash_t;
import mmx.addr_t;

class TokenBase extends mmx.Contract {
	
	string name;
	
	string symbol;
	
	string web_url;
	
	string icon_url;
	
	int decimals = 6;
	
	// TODO: optional<addr_t> meta_data;
	
	
	bool is_valid() const;
	
	hash_t calc_hash(bool full_hash) const;
	
	ulong calc_cost(mmx.ChainParams* params) const;
	
}
