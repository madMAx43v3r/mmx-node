package mmx;

struct peer_info_t {
	
	string address;
	
	uint32 height;				// synced height
	
	long recv_timeout_ms;		// delta since last reply [ms]
	
	bool is_synced;
	
	bool is_blocked;
	
	bool is_outbound;
	
}
