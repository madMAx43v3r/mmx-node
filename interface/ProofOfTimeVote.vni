package mmx;

class ProofOfTimeVote {
	
	uint key_height;					// block height that contains node pubkey
	
	hash_t prev_hash;					// starting point of verified vdf
	
	signature_t node_sig;
	
	
	hash_t calc_hash(bool full_hash = false) const;
	
}
