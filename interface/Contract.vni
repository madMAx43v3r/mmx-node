package mmx;

class Contract {
	
	uint version;
	
	
	bool is_valid() const;
	
	hash_t calc_hash() const;
	
	ulong calc_min_fee(ChainParams* params) const;
	
	vector<hash_t> get_dependency() const;
	
	void apply(Operation* operation);
	
	vector<tx_out_t> validate(Operation* operation, Solution* solution, Context* context) const;
	
}
