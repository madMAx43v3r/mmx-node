package mmx;

struct tx_exec_result_t {
	
	bool did_fail;
	
	uint total_cost;			// static cost + exec cost
	
	vector<txin_t> inputs;
	
	vector<txout_t> outputs;
	
	optional<string> message;
	
	static const uint MAX_MESSAGE_LENGTH = 128;
	
}
