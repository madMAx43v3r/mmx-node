package mmx;

struct spend_options_t {
	
	uint min_confirm = 1;
	
	ulong extra_fee;
	
	optional<addr_t> sender;
	
	map<addr_t, addr_t> owner_map;
	
	map<addr_t, Contract*> contract_map;
	
	optional<map<addr_t, uint128>> budget_map;
	
}
