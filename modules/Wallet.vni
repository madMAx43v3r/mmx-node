package mmx;

module Wallet {
	
	vector<string> key_files;
	
	
	void show_farmer_keys(uint index) const;
	
	FarmerKeys* get_farmer_keys(uint index) const;
	
	vector<FarmerKeys*> get_all_farmer_keys() const;
	
}
